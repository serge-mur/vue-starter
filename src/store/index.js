import { createStore } from 'vuex'
import products from '@/store/products'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    products
  }
})
